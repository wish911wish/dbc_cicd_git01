resources:
  pipelines:
    cc_git_abc_pipeline_bundle_etl:
      name: cc_git_abc_pipeline_bundle_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/cc_git_abc_pipeline_bundle_etl"

      libraries:
        - glob:
            include: ../src/cc_git_abc_pipeline_bundle_etl/transformations/**

      environment:
        dependencies:
          - --editable ${workspace.file_path}
