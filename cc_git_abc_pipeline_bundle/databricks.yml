# This is a Databricks asset bundle definition for cc_git_abc_pipeline_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: cc_git_abc_pipeline_bundle
  uuid: f1f0ff00-ea69-43c7-a331-42fe43c73bc9

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-c774cc06-d9a6.cloud.databricks.com
    variables:
      catalog: dbc_cicd
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://dbc-c774cc06-d9a6.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/wish911wish@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/wish911wish@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dbc_cicd
      schema: prod
    permissions:
      - user_name: wish911wish@gmail.com
        level: CAN_MANAGE
