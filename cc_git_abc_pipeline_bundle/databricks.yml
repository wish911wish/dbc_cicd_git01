bundle:
  name: cc_git_abc_pipeline_bundle
  uuid: f1f0ff00-ea69-43c7-a331-42fe43c73bc9

include:
  - resources/*.yml
  - resources/*/*.yml

variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-c774cc06-d9a6.cloud.databricks.com
    variables:
      catalog: dbc_cicd
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://dbc-c774cc06-d9a6.cloud.databricks.com
      root_path: /Workspace/Users/wish911wish@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dbc_cicd
      schema: prod
    permissions:
      - user_name: wish911wish@gmail.com
        level: CAN_MANAGE
