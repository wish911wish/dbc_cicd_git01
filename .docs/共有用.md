

ローカル実行用のpyspark
https://claude.ai/share/fca40065-b638-4586-a09a-fc863366be6b




```
KEYS = [
    "spark.sql.session.timeZone",
    "spark.sql.ansi.enabled",
    "spark.sql.legacy.timeParserPolicy",
    "spark.sql.datetime.java8API.enabled",
    "spark.sql.execution.arrow.pyspark.enabled",
    "spark.sql.storeAssignmentPolicy",
    "spark.sql.legacy.parquet.datetimeRebaseModeInRead",
    "spark.sql.legacy.parquet.datetimeRebaseModeInWrite",
    "spark.sql.parquet.int96RebaseModeInRead",
]
for k in KEYS:
    try:
        print(f'.config("{k}", "{spark.conf.get(k)}")')
    except Exception:
        print(f"# {k}: unset")

import java.util  # noqa  -- 代わりに↓
print("JVM TZ:", spark.sparkContext._jvm.java.util.TimeZone.getDefault().getID())
print("Spark:", spark.version)
```


```
import time, datetime
print(time.tzname, datetime.datetime.now().astimezone().utcoffset())
print(datetime.datetime.fromtimestamp(-62135596800))   # ここで落ちるか
```

```
# requirements-ci.txt
pyspark[connect]==4.1.0
pyarrow==21.0.0
grpcio==1.67.0
grpcio-status==1.67.0
googleapis-common-protos==1.65.0
pytest==8.3.5
```



```
print(type(df).__module__)   # ...connect... か ...classic... か
```


```

row_a = df.filter(F.col("updated_at") == "2026-02-01").alias("a")
row_b = df.filter(F.col("updated_at") == "2026-01-01").alias("b")

merged = row_a.crossJoin(row_b).select(
    *[F.coalesce(F.col(f"a.{c}"), F.col(f"b.{c}")).alias(c) for c in df.columns]
)

```
